exportPattern("^[[:alpha:]]+")    # falls alle Funktionen fuer den Anwender sichtbar sein sollen
## export(grevilleIt, grevilleRek, leftHandSide, z1Inverse, z1Matrix, mgs, LS_householder, LS_mgs, generateLSProb)     # falls nur bestimmte Funktionen, Datensaetze, usw. fuer den Anwender sichtbar sein sollen
## importFrom(pracma, householder)     # Die Funktion householder aus dem Paket pracma wird benoetigt
importFrom(sensitivity, morris)
importFrom(sensitivity, sobol2002)
